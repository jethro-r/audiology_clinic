// Canonical production host. The site serves on www (non-www redirects to it),
// so every absolute URL we emit — metadataBase, canonicals, sitemap, robots,
// JSON-LD — must use this host.
export const SITE_URL = "https://www.veritashearing.co.nz";
